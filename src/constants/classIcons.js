import { Crosshair, Shield, Axe, Sparkles, HeartPulse } from 'lucide-react'

/** Fallback Lucide quando a imagem da classe não carregar. */
export const CLASS_FALLBACK_ICONS = {
  atirador: Crosshair,
  tank: Shield,
  porradeiro: Axe,
  magica: Sparkles,
  suporte: HeartPulse,
}
